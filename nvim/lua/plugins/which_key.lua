-- ===============================================================
-- Which Key — Keymap hint popup
-- ===============================================================

require("which-key").setup({
    icons = {
        breadcrumb = "",
        separator = "",
        group = "",
    },
})
